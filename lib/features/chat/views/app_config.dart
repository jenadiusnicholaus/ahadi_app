// Re-export the canonical config from `lib/core/config/app_config.dart`.
// This file remains for backward compatibility for imports that reference
// `lib/features/chat/views/app_config.dart`.

// Prefer importing the core config directly:
// import 'package:ahadi/core/config/app_config.dart';

export 'package:ahadi/core/config/app_config.dart';
